{
  "name": "ms_math_solver_api",
  "version": "1.0.0",
  "description": "An opinionated api wrapper of MS Math Solver for my dissertation",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "example": "ts-node-dev example.ts",
    "build": "tsdx build --entry lib/index.ts",
    "start": "tsdx watch",
    "prepublishOnly": "npm run build",
    "prepack": "npm run build",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apisauce": "^2.1.5",
    "googleapis": "^100.0.0",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^17.0.31",
    "dotenv": "^16.0.0",
    "ts-node-dev": "^1.1.8"
  }
}
